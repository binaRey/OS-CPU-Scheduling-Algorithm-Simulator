# OS-CPU-Scheduling-Algorithm-Simulator
A dependency-free CPU Scheduling Simulator built from scratch with vanilla HTML, CSS, JS, and PHP for an OS final project. It utilizes native ES modules for a clean, framework-free architecture, requiring a local server environment (like Apache/XAMPP) to run over HTTP.
